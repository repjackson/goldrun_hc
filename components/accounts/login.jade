template(name='login')
    .ui.padded.stackable.grid
        .centered.row
            .ten.wide.column
                h1.ui.center.aligned.header enter gro
                    //- i.massive.icons
                    //-     i.home.icon
                    //-     i.corner.right.chevron.icon
                if message
                    h4.ui.center.aligned.header #{message}
                .ui.fluid.large.input(class=login_class)
                    .ui.inline.small.header 
                        i.user.icon
                        |username
                    input.username(type='text')
                .ui.fluid.big.input(class=login_class)
                    .ui.inline.small.header 
                        i.lock.icon
                        | password
                    input.password(type='password')
                //- if username
                if logging_in
                    .ui.fluid.big.basic.button.enter(title="Login #{username}" class=enter_class)
                        i.sign.in.alternate.icon
                        |login
                        | #{username}
                        //- i.right.chevron.icon
                else
                    if username
                        .ui.center.aligned.segment
                            .ui.small.header no '#{username}' user
                            a.ui.basic.button(href='/register')
                                i.user.plus.icon
                                | register
                .ui.center.aligned.basic.segment    
                    a.ui.large.basic.button(href='/forgot_password')
                        i.refresh.icon
                        |forgot password
                    a.ui.large.basic.button(href='/register') 
                        i.user.plus.icon
                        |register
