template(name='user_layout')
    //- img.ui.fluid.image.checkin_banner(src="{{c.url 'frontrange' width=1000 height=1000 crop='crop'}}")
    +nav
    with user_from_username_param
        .ui.stackable.grid.global_container
            .row
                img.ui.fluid.image.checkin_banner(src="{{c.url banner_image width=1000 height=1000 crop='crop'}}")
                .four.wide.column
                    if user_is_resident
                        if is_staff
                            if kiosk_photo
                                img.ui.centered.circular.image(src="{{c.url kiosk_photo height=300 width=300 gravity='face' crop='fill'}}")
                            else if image_id
                                img.ui.centered.circular.image(src="{{c.url image_id height=300 width=300 gravity='face' crop='fill'}}")
                        else
                            img.ui.centered.circular.shadowed.image(src="{{c.url profile_image_id height=300 width=300 gravity='face' crop='fill'}}")
                        //- a.item(href="/user/#{user_from_username_param.username}/")
                    else
                        if is_current_user
                            +image_edit key='profile_photo' label='profile photo' direct=true
                    .ui.center.aligned.basic.segment
                        a(href="/user/#{user.username}")
                            h1.ui.large.center.aligned.header
                                strong #{user.first_name} #{user.last_name}
                                small @#{user.username}        
                    if user_is_resident
                        if is_staff
                            a.ui.big.button.item(href="/user/#{user_from_username_param.username}/edit")
                                //- i.cog.icon
                                +i name='settings' 
                                |edit
                            +resident_info         
                            +user_check_steps
                            +staff_verification
                        else 
                            .ui.small.header #{tagline}
                    if is_current_user
                        a.ui.large.button.item(href="/user/#{user_from_username_param.username}/edit" title='profile settings')
                            +i name='settings' 
                            //- i.cog.icon
                            |edit
                    else if is_admin
                        a.ui.large.button.item(href="/user/#{user_from_username_param.username}/edit" title='profile settings')
                            +i name='settings' 
                            //- i.cog.icon
                            |edit
                    if is_dev
                        +boolean_edit key='disabled' label='disabled' icon='user shield' direct='true'
                    //- else
                    //-     .ui.button.direct_message(title='new message')
                    //-         +i name='envelope'
                    if is_current_user
                        a.ui.large.button.logout(href="#" title='logout')
                            //- i.sign.out.alternate.icon
                            +ismall name='exit' 
                            |log out
                        a.ui.large.button.logout_other_clients(href="#" title='logout other clients')
                            //- i.globe.icon
                            +ismall name='globe' 
                            |log out other clients
                    if is_admin
                        .ui.basic.segment
                            .ui.header 
                                +i name='shield'
                                |roles
                            each roles
                                .ui.basic.label #{this}
                .twelve.wide.column.shadowed-box
                    if is_staff
                        if user_is_resident
                            a.ui.button(href="/user/#{username}/healthclub" class="{{isActiveRoute 'user_healthclub'}}")
                                +ismall name='treadmill' classes='ui inline image'
                                |healthclub
                    if is_member
                        a.ui.compact.button(href="/user/#{username}/dashboard" class="{{isActiveRoute 'user_dashboard'}}")
                            +ismall name='dashboard' classes='ui inline image'
                            |dashboard
                        a.ui.compact.button(href="/user/#{username}/about" class="{{isActiveRoute 'user_about'}}")
                            +ismall name='info' classes='ui inline image'
                            |info
                        a.ui.compact.button(href="/user/#{username}/social" class="{{isActiveRoute 'user_social'}}")
                            +ismall name='collaboration' classes='ui inline image'
                            |social
                        a.ui.compact.button(href="/user/#{username}/gallery" class="{{isActiveRoute 'user_gallery'}}")
                            +ismall name='gallery' classes='ui inline image'
                            |gallery
                        a.ui.compact.button(href="/user/#{username}/connections" class="{{isActiveRoute 'user_connections'}}")
                            +ismall name='handshake' classes='ui inline image'
                            |connections
                        a.ui.compact.button(href="/user/#{username}/bookmarks" class="{{isActiveRoute 'user_bookmarks'}}")
                            +ismall name='star' classes='ui inline image'
                            |bookmarks
                        a.ui.compact.button(href="/user/#{username}/contact" class="{{isActiveRoute 'user_contact'}}")
                            +ismall name='phone' classes='ui inline image'
                            |contact
                        a.ui.compact.button(href="/user/#{username}/events" class="{{isActiveRoute 'user_events'}}")
                            +ismall name='calendar' classes='ui inline image'
                            |calendar
                        a.ui.compact.button(href="/user/#{username}/comparison" class="{{isActiveRoute 'user_comparison'}}")
                            +ismall name='scales' classes='ui inline image'
                            |comparison
                        a.ui.compact.button(href="/user/#{username}/offers" class="{{isActiveRoute 'user_offers'}}")
                            +ismall name='sell' classes='ui inline image'
                            |offers
                        a.ui.compact.button(href="/user/#{username}/tags" class="{{isActiveRoute 'user_tags'}}")
                            +ismall name='tags' classes='ui inline image'
                            |tags
                        a.ui.compact.button(href="/user/#{username}/tasks" class="{{isActiveRoute 'user_tasks'}}")
                            +ismall name='tasks' classes='ui inline image'
                            |tasks
                        a.ui.compact.button(href="/user/#{username}/messages" class="{{isActiveRoute 'user_messages'}}")
                            +ismall name='new-message' classes='ui inline image'
                            |mail
                        a.ui.compact.button(href="/user/#{username}/karma" class="{{isActiveRoute 'user_karma'}}")
                            +ismall name='yin-yang' classes='ui inline image'
                            |karma
                        a.ui.compact.button(href="/user/#{username}/workhistory" class="{{isActiveRoute 'user_workhistory'}}")
                            +ismall name='resume' classes='ui inline image'
                            |work history
                        a.ui.compact.button(href="/user/#{username}/stats" class="{{isActiveRoute 'user_stats'}}")
                            +ismall name='line-chart' classes='ui inline image'
                            |stats
                        a.ui.compact.button(href="/user/#{username}/votes" class="{{isActiveRoute 'user_votes'}}")
                            +ismall name='elections' classes='ui inline image'
                            |votes
                        a.ui.compact.button(href="/user/#{username}/notifications" class="{{isActiveRoute 'user_notifications'}}")
                            +ismall name='appointment-reminders' classes='ui inline image'
                            |notifications
                        div
                    +yield
    unless is_frontdesk
        //- unless is_staff
        +footer 


template(name='user_check_steps')
    each user_check
        unless user_check_completed
            .ui.compact.small.button.recheck
                if user_check_completed
                    i.large.checkmark.icon
                    |#{title}
                else 
                    i.large.circle.outline.icon 
                    |#{fail_text}
                //- i.icon(name=icon)
                //- +i name=icon classes='ui avatar image'
    if checkins_without_gov_id
        if is_staff
            .ui.tiny.header #{checkins_left_without_gov_id} check in's left without government ID
        else
            .ui.header #{checkins_left_without_gov_id} check in's left without government ID
    if checkins_without_email_verification
        if is_staff
            .ui.tiny.header #{checkins_left_without_email_verification} check in's left without email verification
        else
            .ui.header #{checkins_left_without_email_verification} check in's left without email verification
        +email_validation_check





template(name='staff_verification')
    if is_staff
        .ui.basic.segment
            .ui.header 
                if staff_verifier
                    +i name='id-verified' classes='ui mini inline image'
                else
                    i.red.ban.icon
                |verified by #{staff_verifier} on {{long_date verification_timestamp}}
            unless staff_verifier
                .ui.button.verify verify government ID as #{currentUser.username}
            .ui.small.header #{user_from_username_param.username} created by #{user_from_username_param.added_by_username} on {{long_date user_from_username_param.createdAt}}



template(name='user_residency')
    +resident_info         
    +user_key
    +user_unit
    .ui.basic.segment
        //- +text_view key='first_name' label='First Name' direct=true
        //- +text_view key='last_name' label='Last Name' direct=true
        if is_admin
            +text_view key='address' label='address' direct=true
        //- +text_view key='email' label='Email' direct=true
        +phone_view key='phone' label='phone' icon='phone' direct=true
        if user_is_resident
            //- if is_staff
            +number_view key='unit_number' label='unit #' direct=true
            +number_view key='building_number' label='building #' direct=true
            +number_view key='healthclub_code' label='healthclub_code' direct=true
            +text_view key='building_code' label='building code' direct=true
            +date_view key='start_date' label='start date' icon='overtime' direct=true
            +date_view key='expiration_date' icon='calendar-minus' label='lease expires' direct=true
        +textarea_view key='notes' label='notes' icon='align-left' direct=true
        each emails
            .ui.label #{address}
    
    
template(name='user_healthclub')
    .ui.basic.segment
        //- .ui.header 
        //-     +i name='token-card-code'
        //-     |hc card
        //- .ui.circular.button.generate_barcode
        //-     +i name='barcode' classes='ui inline image'
        //-     |generate health club checkin barcode
        //- div
        //- svg#barcode
    
template(name='profile_home')
    //- if user_is_owner
    //-     +owner_profile
    if user_is_resident
        +resident_about    
    
    
template(name='owner_profile')
    .ui.header owner profile    
    each roles
        .ui.large.label #{this}
    
    
template(name='resident_about')
    img.ui.fluid.rounded.image(src="{{c.url banner_image_id width=1000 height=200 crop='crop'}}")
    .ui.stackable.grid
        .three.column.row
            //- if user_from_username_param.healthclub_code
            //-     .column
            //-         +user_healthclub                        
            if user_is_resident
                if is_staff
                    each staff_resident_widgets
                        .column
                            +Template.dynamic template=widget_template
                    .column
                        +user_checkins
                    .column
                        +user_guests
                    //- .column
                    //-     +user_violations
                    //- .column
                    //-     +user_signatures
                    .column
                        +user_notes        
                    .column
                        +user_unit
                    .column
                        +membership_status
                    .column
                        +user_permit
                
template(name='resident_info')
    .ui.basic.segment
        //- +text_view key='first_name' label='First Name' direct=true
        //- +text_view key='last_name' label='Last Name' direct=true
        if is_admin
            +text_view key='address' label='address' direct=true
        //- +text_view key='email' label='Email' direct=true
        +phone_view key='phone' label='phone' icon='phone' direct=true
        if user_is_resident
            //- if is_staff
            +number_view key='unit_number' icon='room' label='unit #' direct=true
            +number_view key='building_number' icon='building' label='building #' direct=true
            +number_view key='healthclub_code' label='healthclub code' direct=true
            +text_view key='building_code' label='building code' direct=true
            +date_view key='start_date' label='start date' direct=true
            +date_view key='expiration_date' label='expiration date' direct=true
        +textarea_view key='notes' label='notes' icon='align-left' direct=true
        div
        each emails
            if verified
                .ui.large.basic.label(title='verified') #{address}
            else
                .ui.orange.large.basic.label(title='unverified') #{address}

    
            
template(name='user_permit')
    .ui.basic.segment
        .ui.inline.center.aligned.header 
            |permit
        +goto_model slug='parking_permit'
        
        with permit_doc
            .ui.inline.header Permit
            .ui.inline.header #{permit}
            div
            .ui.inline.header Date Issued
            .ui.inline.header #{date_issued}
            div
            .ui.inline.header Expiration Date
            .ui.inline.header #{expiration_date}
            div
            .ui.inline.header License Plate 
            .ui.inline.header #{license_plate}
            div


            
template(name='user_permits')
    if user_permits
        .ui.basic.segment
            .ui.inline.center.aligned.header 
                i.id.card.alternate.icon
                +goto_model slug='parking_permit'
                |permits

            
template(name='user_signatures')
    .ui.basic.segment
        .ui.inline.center.aligned.header 
            i.pencil.icon
            | Signatures
        .ui.link.large.list
            a.item(href='#') Rules and Regulations
            +goto_model slug='rules_regs_signing'

            a.item(href='#') Proof of Residency
            a.item(href='#') Injury Waiver


template(name='user_notes')
    if user_is_resident
        if is_staff
            +textarea_edit key='staff_notes' title='private staff notes' icon='note' direct=true
            +textarea_edit key='public_staff_notes' title='public staff notes (shown on checkin screen)' icon='note' direct=true

            
            
template(name='user_checkins')
    .ui.basic.segment
        .ui.inline.center.aligned.header 
            |health club check ins
        if is_staff
            +checkin_button
        .ui.list.smallscroll
            each healthclub_checkins
                .item #{when}
            
            
            
template(name='user_guests')
    .ui.basic.segment
        .ui.inline.center.aligned.header 
            i.users.icon
            |guests
        +goto_model slug='guest'
        //- +add_button model='guest'
        //- .ui.circular.button.add_guest
        //-     |Add Guest
        //- with user_from_username_param
        //-     .ui.header total guests #{total_guests}
            //- .ui.header guests this month #{current_month_guests}
        .ui.link.large.list
            each resident_guests
                a.item(href="/m/guest/#{_id}/view")
                    img.ui.avatar.image(src="{{c.url image_id width=100 height=100 gravity='face' crop='fill'}}")
                    .content
                        |#{guest_name} #{first_name} #{last_name} #{username}

            
            
template(name='user_unit')
    .ui.basic.segment
        .ui.inline.center.aligned.header 
            i.home.icon
            |unit
        +goto_model slug='unit'
        with unit
            +number_view key='building_number' label='building number' direct=true
            +number_view key='unit_number' label='unit #' direct=true
            if is_admin
                div
                +edit_button
                +view_button

            
            
            
template(name='user_section')
    +user_delta
    // +Template.dynamic template=user_section_template

    
template(name='user_violations')
    .ui.basic.segment
        .ui.inline.center.aligned.header 
            |violations
        +goto_model slug='violation'
        .ui.icon.button.add_inline_violation(title='add violation')
            i.large.plus.icon
            //- |add violation
        if editing_violation
            .ui.basic.segment
                with editing_violation_doc
                    +textarea_edit key='body' label='description' direct=true
                    +boolean_edit key='active' label='active' direct=true
                    +date_edit key='violation_date' label='date' direct=true
                    +time_edit key='violation_time' label='time' direct=true
                    i.checkmark.link.circular.icon.save_violation
        //- .ui.list
        //-     each violations
        //-         .item
        //-             img.ui.avatar.image(src="{{c.url image_id height=100 gravity='face' crop='fill'}}")
        //-             .content
        //-                 .header #{title}
        //-                 .description !{description}
        //-                     |date:#{violation_date}
        //-                     |time: #{violation_time}
        .ui.comments
            each violations
                .comment
                    img.ui.avatar.image(src="{{c.url image_id height=100 gravity='face' crop='fill'}}")
                    .content
                        a.header(href="/m/violation/#{_id}/view") !{description}
                        .meta #{when}
                            +textarea_view key='body' label='description' direct=true
                            div Date: #{violation_date}
                            div Time: #{violation_time}
                            if active
                                .ui.red.label Active
                        .actions
                            i.pencil.link.circular.icon.edit_violation



template(name='user_log')
    //- .ui.fluid.styled.accordion
    .ui.basic.segment
        .ui.inline.center.aligned.header 
            //- +i name='overview-pages-4'
            i.list.icon
            |log
        scroll
            .ui.list
                each user_log_events
                    .item
                        .content
                            .header #{body}
                            .description #{when}


template(name='user_rules_widget')
    .ui.cloud.segment
        .ui.header
            i.signature.icon
            | user rules signature
    

                
template(name='user_array_list')
    .ui.list
        each users
            .item
                +user_info
            
template(name='user_comparison')
    .ui.header user comparison
    //- .ui.header simulate conversation between you and them using site stats

            

            
template(name='user_array_element_toggle')
    .ui.circular.button.toggle_element(class=user_array_element_toggle_class)
        |#{value}

template(name='user_string_toggle')
    .ui.circular.button.toggle_element(class=user_string_element_toggle_class)
        |#{value}
        
            
            
template(name='membership_status')
    .ui.basic.segment
        .ui.inline.center.aligned.header 
            |membership status
        +email_validation_check
        +lease_expiration_check
        +rules_and_regs_check
        .ui.large.basic.button.email_rules_receipt
            i.envelope.teal.icon
            |email rules and regs receipt
        //- +rules_and_regs_
        if user_is_owner
            +image_view key='ownership_paperwork' label='ownership paperwork' direct=true
            +pdf_view key='lease_agreement' label='lease agreement' direct=true
        else if user_is_resident
            +date_view key='expiration_date' label='expiration date' direct=true
            div
            +pdf_view key='lease_agreement' label='lease agreement' direct=true
            div
            +pdf_view key='lease_addendum' label='lease addendum' direct=true
