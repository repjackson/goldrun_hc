.ui.inverted.borderless.stackable.menu
    a.toggle_sidebar.mobile.only.item
        i.sidebar.icon
        | Gold Run

    a.header.mobile.hidden.item(href='/' class="{{isActivePath '/'}}") 
        i.home.icon
        |Gold Run
    // .header.mobile.hidden.item GR
    a.mobile.hidden.item(href='/readings' class="{{isActivePath '/readings'}}")
        i.theme.icon
        | Readings
    a.mobile.hidden.item(href='/lostfound' class="{{isActivePath '/lostfound'}}")
        i.random.icon
        |Lost and Found
    a.mobile.hidden.item(href='/bikes' class="{{isActivePath '/bikes'}}")
        i.bicycle.icon
        |Bikes
    a.mobile.hidden.item(href='/frontdesk' class="{{isActivePath '/frontdesk'}}")
        i.circle.info.icon
        |Desk Staff
    a.mobile.hidden.item(href='/buildings' class="{{isActivePath '/buildings'}}")
        i.building.icon
        |Buildings
    a.mobile.hidden.item(href='/tasks' class="{{isActivePath '/tasks'}}")
        i.edit.icon
        |Tasks
    a.mobile.hidden.item(href='/keys' class="{{isActivePath '/keys'}}")
        i.key.icon
        |Keys
    a.mobile.hidden.item(href='/people' class="{{isActivePath '/people'}}")
        i.users.icon
        |People
    .mobile.hidden.right.menu
        if currentUser
            // a.icon.item(href='/dashboard' class="{{isActivePath '/dashboard'}}")
            //     i.line.chart.icon
            // .ui.simple.dropdown.icon.item
            //     i.alarm.icon
            //     .menu
            //         each notifications
            //             .item #{text}
            // a.icon.item(href='/messages' class="{{isActivePath '/messages'}}")
            //     i.mail.icon
            if isInRole 'admin'
                a.icon.mobile.hidden.item(href='/admin/members' class="{{isActivePath '/admin/members'}}")
                    i.configure.icon
                    |Admin
            a.mobile.hidden.item(href="/account/profile/edit/#{currentUser._id}" class="{{isActivePath '/account/profile/edit/#{currentUser._id}'}}")
                if currentUser.profile.image_id
                    img.ui.avatar.image(src="{{c.url currentUser.profile.image_id width=50 height=50 gravity='face' crop='fill'}}")
                else
                    i.user.icon
                if currentUser.name
                    | #{currentUser.name}
                else
                    | Profile
            a#logout.ui.mobile.hidden.item(href='#') 
                i.sign.out.icon
                | Sign Out
        else
            a.ui.mobile.hidden.item(href='/sign-in') 
                i.sign.in.icon
                | Sign In
